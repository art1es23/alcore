const pageGlue = (container) => {

    // class TabToggle {
    //     constructor(props) {
    //         this.listTab = props.element;

    //         this.items = Array.from(this.listTab.querySelectorAll('.glue-list__item'));

    //         this.init();
    //     }

    //     init() {
    //         this.toggleTabs();
    //     }

    //     toggleTabs() {
    //         this.items.forEach(item => {
    //             const trigger = item.querySelector('.glue-list__title');
    //             const text = item.querySelector('.glue-list__description');

    //             item.addEventListener('click', (e) => {

    //                 text.classList.remove('hide');
    //                 text.classList.add('show');
    //             });

    //             text.classList.remove('show');
    //         });
    //     }

    // }

    // let someTabs = new TabToggle({
    //     element: container.querySelector('.glue-list')
    // });
};

export default pageGlue;